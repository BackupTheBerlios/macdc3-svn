{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {connect = id; disconnect = id; send = id; }; 
            CLASS = MyDocument; 
            LANGUAGE = ObjC; 
            OUTLETS = {commandField = id; textField = id; }; 
            SUPERCLASS = NSDocument; 
        }
    ); 
    IBVersion = 1; 
}